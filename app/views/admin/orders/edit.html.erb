<div class="container">
	<br>
	<!-- Navigation -->
	 <nav class="navbar navbar-expand-sm   navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/admin">Admin panel <span class="sr-only">(current)</span></a>
        </li>
      </ul>
      <div>
        <%= link_to('Logout', destroy_admin_session_path, method: :delete, :class => 'btn btn-danger btn-sm') %>  
      </div>
    </div>
  </nav>
  <div class="order_tracking u-dimension-2">
	  <h4> Order tracking details update </h4><hr>
		<%= form_for @order, url:{action: :update} do |f| %>
	    <%= f.select :order_status, [ 'Processed','Shipped','En Route', 'Arrived' ], :prompt => 'Select One', :class => 'form-control' %> &nbsp;&nbsp; &nbsp;&nbsp; &nbsp;&nbsp;
	    <%= f.submit "Update", :class => 'btn btn-success' %>
    <% end %>
	</div>
	<p style="text-align: center;"><%= link_to 'Back', admin_orders_path %></p>
</div>

<style type="text/css">
	
</style>